import java.util.Scanner;

// Solves: https://leetcode.com/problems/maximum-subarray/description/
public class MaximumSubarray {

    static void main(String[] args) {
        Scanner sc= new Scanner(System.in);
        /*
            Brute force method:
                We consider a triple nested loop
                Outer loop will iterate through array from 0 to n-1
                Inner loop will iterate through array from n-1 to 0
                    Inner-Inner loop will iterate through array from outer to inner loop range
                    calculate sum for each subarray created and highest is returned
         */
    }

}
